rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Demo/testing only. Replace with role-based production rules before deployment.
    match /{document=**} { allow read, write: if request.auth != null; }
  }
}
